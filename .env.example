# Public project URL (safe for browser exposure)
NEXT_PUBLIC_SUPABASE_URL=

# Server-only. Never expose this value to the browser.
SUPABASE_SERVICE_ROLE_KEY=

# Protects /api/cron/sync. Vercel Cron sends Authorization: Bearer <CRON_SECRET>.
CRON_SECRET=

# mock | etimad-public
DATA_SOURCE=mock

# Official Etimad Open Data API configuration.
# Obtain the real server URL and tenders groupId from the official Etimad API/Open Data portal.
# The public integration guide documents: GET {BASE_URL}/etimad/v1/opendata/{groupId}?fileFormat=2
ETIMAD_OPEN_DATA_BASE_URL=
ETIMAD_OPEN_DATA_GROUP_ID=
ETIMAD_OPEN_DATA_FILE_FORMAT=2

# Legacy direct endpoint override. Optional and kept for backwards compatibility.
# When set, it takes precedence over ETIMAD_OPEN_DATA_BASE_URL / ETIMAD_OPEN_DATA_GROUP_ID.
ETIMAD_PUBLIC_API_URL=
